{
  "name": "hyperspan",
  "private": true,
  "workspaces": [
    "packages/*"
  ],
  "version": "2.0.0-alpha.1",
  "scripts": {
    "test": "vitest run",
    "test:bun": "bun test",
    "publish:packages": "node scripts/publish-packages.mjs"
  },
  "devDependencies": {
    "typescript": "^5.9.3",
    "vitest": "^3.2.4"
  }
}
